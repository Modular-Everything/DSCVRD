// * NOTE
// ** Ideally the projectId and dataset would be imported from
// ** the .env file but I can't get that working, so they're
// ** hardcoded here instead.

// TODO
// Maybe I could import the gatsby-config file and grab the
// date from there instead? Don't worry about it for now though.

export default {
  sanity: {
    projectId: 'lylk5ufs',
    dataset: 'production',
  },
};
